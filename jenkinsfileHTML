pipeline {
    agent any
    stages {
        stage ('Checkout') {
            steps {
                git 'https://github.com/JitendraSies/javarep.git'
            }
        }
        stage ('Publish') {
            steps {
                publishHTML ([
                    allowMissing: true,
                    alwaysLinkToLastBuild: false,
                    keepAll: false,
                    reportDir: '.',
                    reportFiles: 'file1.html',
                    reportName: 'My HTML Page'
                ])
            }
        }
    }
}
